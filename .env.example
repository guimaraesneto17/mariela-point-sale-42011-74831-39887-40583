# MongoDB Atlas Connection String
# Substitua <username>, <password>, <cluster> e <database> pelos seus valores
# Exemplo: mongodb+srv://usuario:senha123@cluster0.mongodb.net/mariela-pdv?retryWrites=true&w=majority
MONGODB_URI=mongodb+srv://<username>:<password>@<cluster>.mongodb.net/<database>?retryWrites=true&w=majority

# Porta do servidor (opcional, padrão: 3001)
PORT=3001

# Ambiente (development, production)
NODE_ENV=development
